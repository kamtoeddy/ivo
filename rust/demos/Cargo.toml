[package]
name = "demos"
version = "0.1.0"
edition = "2024"

[dependencies]
ivo = { path = "../ivo", features = ["validators"] }
regex = "1.10"
unicode-normalization = "0.1"
tokio = { version = "1.0", features = ["full"] }
async-std = { version= "1.13.2", features = ["attributes"] }
smol = "2.0.2"
