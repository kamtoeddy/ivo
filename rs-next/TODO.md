- [ ] fix `## 17. Execution pipeline` in GOAL.md
  - [x] the validation, re_validation, and sanitization of virtual fields does not happen unless a virtual field is provided and accepted (i.e: not ignored) — fixed for both `create` and `update`; see `build_virtual_pipeline` in `crates/derive/src/lib.rs`. Covered by `should_only_sanitize_virtual_fields_that_were_provided` and `should_not_re_validate_virtual_fields_that_were_not_provided_or_were_ignored` in `tests/fields/virtuals/mod.rs`.
  - [x] re_validation does not happen unless a field has a revalidator and was successfully validated — virtual fields now support `#[re_validate]` (previously silently ignored); gated on the field's own validate having succeeded. Covered by `should_properly_use_re_validated_values` (create + update).
  - [x] the sanitization of virtuals only happens after a successful post-validation — sanitize step moved to run after `post_validate` and the errors check, for both `create` and `update`. Covered by `should_sanitize_virtual_fields_only_after_post_validate_succeeds`.
  - [x] make sure every major operation in Model.create and Model.update should happen in parallel — added `emit_async_phase` (`crates/derive/src/lib.rs`), a shared codegen helper: 0/1 async handler in a phase stays sequential (matches GOAL.md §15: no `join!`/allocation overhead for sync-only schemas), 2+ async handlers are polled concurrently via `futures_util::join!` (stack-only, no boxing, unlike `join_all`). Applied to every independent-handler phase:
    - validate (create + update) and re-validate (create + update): required/lax and virtual fields are one *combined* phase each, not a virtual pass followed by a separate required/lax pass — `build_virtual_pipeline` now returns raw `Vec<AsyncPhaseItem>` (via a `VirtualPipeline` struct) instead of pre-emitted phases, so the caller merges them with the required/lax items before a single `emit_async_phase` call. Sanitize has no non-virtual counterpart, so it stays its own phase.
    - dependent-field resolution (create: per dynamic round; update: per static dependency-graph level, since update resolves in one topological pass rather than a convergence loop)
    - `create`'s required/lax validate phase, now split out from `#[constant]` attachment — constants moved to their own phase running _after_ dependent resolution (fixing a real pre-existing ordering bug: constants were being attached before dependents resolved, contradicting GOAL.md §17's own step order; `ctx.values()` inside a constant resolver now actually sees resolved dependents)
    - lifecycle hooks: `on_success`/`on_failure` (create + update) and `on_delete` — multiple hooks of the same kind are independent by design
    - `post_validate`: restructured to the reference implementation's (`rs/`) two-phase, all-groups-at-once batching (all groups' `pre_validate`s batched together and applied, then all groups' `validate`s batched together) — **deliberate behavior changes**, both pinned by tests: (1) once any group's `pre_validate` fails, no group's main `validate` runs at all (previously each group ran pre-then-main back-to-back independent of other groups' failures); (2) a group's handlers now see a snapshot from _before_ the phase rather than a sibling group's already-applied updates (no existing test relied on cross-group visibility, so this was a silent, apparently-unintended coupling of otherwise-independent groups)
    - ignore evaluation (create + update): grouped `#[ignore(...)]`/`#[ignore_update(...)]` and field-level `#[ignore(...)]`/`#[ignore_update(...)]` are now resolved "in one go" in a single batch, regardless of field type or which kind of ignore option they came from — matching `rs/`'s `filter_input_fields_allowed`, which pools every ignore resolver (entity-level, field-level, grouped) into one `join_all`, rather than rs-next's previous separate sequential passes per option kind
    - required evaluation (create + update): same "one go" batching for grouped `#[required(...)]` and field-level `#[required(...)]`, matching `rs/`'s `evaluate_missing_required_fields`. Note only *conditional* required rules (on lax/virtual fields, and grouped) apply to updates — the bare `#[required]` field type is still creation-only.
    - Every phase above got a dedicated rendezvous-based concurrency-proof test (fails if forced back to sequential; verified by temporarily forcing the threshold and confirming failure). One real bug caught in the process: two of these tests (ignore, required) initially shared one static counter across their create/update variants, which run concurrently under `cargo test` by default — this let one test's resolver spuriously satisfy the other's rendezvous and produce a false pass under the forced-sequential check. Fixed by giving each test its own dedicated schema/counter; worth keeping in mind for any future rendezvous-style test.
    - The validate/re-validate merge is covered by `should_validate_required_and_virtual_fields_in_one_combined_phase` in `tests/fields/virtuals/mod.rs`, which mixes a required field's and a virtual field's async handler in the same rendezvous and checks it (not just "async in general") — verified to fail when forced sequential.
  - [x] for every Model.create/Model.update call, the timestamp resolver should be called at most once — `create` now resolves a shared `__ivo_timestamp_value` once up front (when `#[created_at]`/non-optional `#[updated_at]` are present) instead of calling the resolver per field; `update` only ever had at most one timestamp field to begin with (schemas allow only one `#[updated_at]`), so it was already at most once. Covered by `should_call_the_timestamp_resolver_at_most_once_per_create_call` in `tests/fields/timestamps.rs`.
- [x] Bare `#[ignore]` is not a valid config and all associated logic should be removed
- [x] the ignore_update option should reject empty fields, and consider a closure without fields as an entity-level ignore update handler
- [ ] find all `SKIPPED:` ports and make sure they are valid
- [ ] manually go through all tests in "/rs" and make sure they have been ported correctly
- [ ] update benchmarks, run benchmarks, compare results and improve performance

# added later

- [x] Ported the same fix to `update` (turned out virtual fields got **zero** validate/re-validate/sanitize treatment during update before — a real gap beyond what TODO.md called out). Tests added: `should_properly_use_re_validated_values` and `should_respect_sanitizers_if_provided` both exercise create+update.
- [ ] GOAL.md §10 documents named consts (e.g. `const NAME_EMAIL_REQUIRED: () = ();`) as a supported alternative to `const _: () = ();`, but the macro's parser only recognizes const names starting with `_` and silently drops anything else. I didn't fix it (out of scope for parallelization), but it belongs in the `SKIPPED`
- [ ] limit allocations as much as possible
