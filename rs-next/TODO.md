- [x] fix `## 17. Execution pipeline` in GOAL.md
  - [x] virtual fields' validate/re_validate/sanitize now correctly gated: skipped unless provided & not ignored (validate/re_validate), and sanitize only runs after `post_validate` succeeds. See `build_virtual_pipeline` in `crates/derive/src/lib.rs`; covered in `tests/fields/virtuals/mod.rs`.
  - [x] every independent-handler phase in `create`/`update` now runs in parallel via `emit_async_phase` (sequential for 0/1 async handler, `join!`-batched for 2+, matching GOAL.md §15): validate+re-validate (required/lax and virtual merged into one phase each), dependent resolution, constants, timestamps, lifecycle hooks, `post_validate` (two-phase batching matching `rs/`), ignore evaluation, required evaluation. Also fixed real pre-existing ordering bugs along the way: constants were attached before dependents resolved, and timestamps before constants (both now correctly ordered per GOAL.md §17). Each phase has a dedicated rendezvous-based concurrency-proof test in the relevant `tests/fields/*` file.
  - [x] timestamp resolver now called at most once per `create`/`update` call (previously once per field). Covered by `should_call_the_timestamp_resolver_at_most_once_per_create_call` in `tests/fields/timestamps.rs`.
  - [x] `dependent_create_block` (create's dependent-field resolution) was hand-built as an unconditional `if/else` that always emitted a `HashSet` declaration + resolution loop, even for schemas with zero `#[depends_on]` fields -- the loop degenerated to a no-op (breaks on its first iteration), but the scaffold was still generated and compiled, contradicting GOAL.md §17's "a schema with no `#[depends_on]` fields does not generate the dependent-resolution loop" guarantee. Its update-side sibling (`dependent_update_assignments`) was already correctly empty in this case (it's a plain `Vec<TokenStream>`, one entry per dependency level, naturally empty with no levels). Fixed by short-circuiting `dependent_create_block` to `quote!{}` when there are no dependent fields. Verified via `cargo expand` on a zero-dependent-field schema, before/after the fix, confirming the scaffold is actually gone from the expansion (a compile-only check wouldn't catch this, since the old code was already correct at runtime, just wasteful in codegen). Audited every other phase-producing variable against GOAL.md §17's "Dynamic pipeline generation" checklist (constants, timestamps, ignore/ignore_init/ignore_update, required, validate, re-validate, post_validate, sanitize) -- all already correctly collapse to `quote!{}` when inapplicable via `emit_async_phase`'s existing `items.is_empty()` early return; no further bugs found.
  - [x] condensed the ignore-phase splices (flag declarations + batched evaluation + `ignore_init`/bare-`ignore_update` overrides -- previously 3 separate splices each in `create`/`update`) and the required-phase splices (batched evaluation + bare-`#[required]` missing-field checks, create only) into one named phase variable each (`create_ignore_phase`/`update_ignore_phase`, `create_required_phase`), for readability -- pure codegen-structure change, no behavior difference (each constituent was already correctly empty-when-inapplicable, so the combined variable is too).
  - [x] rewrote GOAL.md §17's Create/Update phase lists to match the actual current pipeline order precisely (they'd drifted from reality across many earlier fixes this session): fail-fast noted after every phase, `post_validate` split into its real pre/main two-phase batching, and update's "evaluate update validity" corrected from appearing after *every* phase (validate, re-validate, post-validate, sanitize individually, as the stale doc claimed) to what the code and `rs/` both actually do -- once, right after post-validate. All 3 update nothing-to-update checkpoints are now called out by number matching `rs/`'s `handle_nothing_to_update_error`.
  - Full suite after all of the above: 809 + 46 passing, unchanged (pure refactor + dead-code removal, no intended behavior change); `examples/main_demo` still runs end-to-end cleanly.
  - [x] merged validate/re-validate/post-validate-pre/post-validate-main with their trailing fail-fast error check into one self-contained phase variable each, instead of the caller splicing `#create_error_check`/`#update_error_check` separately after every one. `update`'s `virtual_ignore_update_attempts` (sets `__update_attempted` for ignored-but-provided virtual fields) got folded into `update_validate_steps` too, right before its internal check, matching its original position exactly -- safe because it never touches `errors`, so its value is moot on any early-return path anyway. One deliberate exception: `update`'s post-validate *main* phase does **not** get its check merged in -- the real check for it already ran later, after the `__changes`/`input` recompute that follows the phase (not immediately after it), because `on_failure` triggers read `__changes` at the point of failure and merging the check in earlier would make them observe stale, pre-recompute state. Left exactly where it already was.
  - [x] moved virtual-field "provided" tracking's one cross-cutting consumer -- the mid-pipeline nothing-to-update checkpoint's "is any virtual field still relevant" check -- from an ad-hoc reconstruction (re-deriving each `__virtual_provided_{name}` identifier from a field name string, done at a distance from where the flags are actually declared) to a new `VirtualPipeline::still_relevant_expr` field, computed in `build_virtual_pipeline` right alongside the flags themselves and consumed via `update_virtual.still_relevant_expr`. (rs/'s `validate()`/`re_validate()` (`rs/src/model/mod.rs:700`,`:800`) don't literally compute "provided" per-function either -- they read a `FieldInfoCollection` computed once beforehand, same shape as rs-next's `setup_stmts`; the actual improvement here is eliminating the fragile string-based identifier reconstruction, not moving the flags' computation itself, since every other consumer -- validate/re-validate/sanitize items inside `build_virtual_pipeline` -- already read them locally where they're declared.)
  - Verified via `cargo expand` on `examples/main_demo`'s schema (validate, re-validate, post-validate pre+main, sanitize, and update's checkpoint 2 all present) that the generated fail-fast checks are each still exactly where they should be: one immediately after validate, one after re-validate, one after post-validate-pre, and (for `update`) the deferred one after the recompute -- no duplicates, no gaps.
  - [x] follow-up: the required phase (`create_required_phase`/`update_required_evaluations`) had been left out of the merge above -- still followed by a separately-spliced `#create_error_check`/`#update_error_check`. Folded in the same way as the other phases, for consistency.
  - [x] follow-up: `update`'s entire post-validate handling -- the `__post_input` scratch setup, the pre/main phases, the `__changes`/`input` recompute, and the final (deliberately deferred, see above) fail-fast check -- was still loose top-level body code rather than a phase variable in its own right. Consolidated into one `update_post_validate_phase` variable (built from the already-existing, already-correctly-ordered pieces, so no behavior changed) and spliced as a single `#update_post_validate_phase`.
  - Verified again via `cargo expand` on `examples/main_demo`'s `update` method that the check ordering is unchanged after both follow-ups: required's check right after required, validate's right after validate, re-validate's right after re-validate, then the post-validate phase's scratch setup -> pre (with its own check) -> main -> recompute -> the one deferred check -> checkpoint 2.
  - Full suite: 809 + 46 passing, unchanged; `examples/main_demo` still runs end-to-end cleanly. Pure codegen-structure refactor -- no intended behavior change.
- [x] Bare `#[ignore]` is not a valid config; removed all associated logic.
- [x] `ignore_update` rejects empty field arrays; a closure with no fields is the entity-level handler.
- [x] fail-fast: `create`/`update` now return immediately after each phase (missing-required, validate, re-validate, post-validate pre+main) if `errors` is non-empty, instead of letting later phases run against already-invalid data. Covered in `tests/fields/required.rs`.
- [x] `update` now matches all 3 of `rs/`'s `handle_nothing_to_update_error` checkpoints (right after ignore/readonly filtering; after post_validate strips unchanged fields; after dependent resolution), not just the last one. Covered in `tests/fields/required.rs`.
- [x] audited all `SKIPPED:` ports; both were stale notes, not real design gaps -- the described behavior was already supported, just never ported/tested. Fixed in `tests/fields/required.rs` and `tests/options/mod.rs`/GOAL.md §19 (`on_success`'s empty-array form is deliberately rejected; "always fire" is the bare arrayless form only).
- [x] `IvoContext::raw_input()` now tracks the schema's original partial input, genuinely distinct from `input()` (previously just an alias). `src/internals/types.rs` gained a real `raw_input` field; `crates/derive/src/lib.rs` threads the correct original-input snapshot through all `IvoContext::new(...)` call sites (create/update phases and lifecycle-hook triggers), plus strips unchanged `#[required]`/`#[lax]` fields from `input()` on update (matching `rs/`'s `evaluate_update_validity`) while `raw_input()` keeps showing what was submitted. Covered by `tests/extras/raw_input.rs`; corrected two pre-existing tests in `tests/fields/{lax,required}.rs` that had been written against the old, wrong behavior.
- [x] `#[post_validate(...)]` errors on an _aliased_ virtual field were silently dropped (allow-list filtered by internal field name, but the error payload is keyed by the alias) -- found via a real panic in `examples/main_demo`. Fixed in `crates/derive/src/lib.rs` via a new `external_field_name()` helper. Covered by `should_surface_post_validate_errors_on_an_aliased_virtual_field` in `tests/fields/virtuals/mod.rs`.
- [ ] manually go through all tests in "/rs" and make sure they have been ported correctly
- [x] audited alias (`#[ivo_virtual("alias")]`) handling at every phase per `rs/tests/fields/virtuals`. Found and fixed:
  - **Three whole test files were silently dead code**: `tests/fields/virtuals/{ignore,on_success,on_failure}.rs` were never `mod`-declared anywhere, so ~276 tests (including all existing alias coverage for those phases) never ran. Fixed by wiring them into `tests/fields/virtuals/mod.rs`.
  - Wiring them in surfaced 8 real pre-existing porting bugs in `on_failure.rs`'s alias tests (asserted the wrong error key -- internal name instead of alias). Fixed.
  - Found the same "internal name instead of alias" bug class in three more derive-macro call sites (virtual field's own validate/re_validate errors, field-level `#[required(handler)]` on virtual fields, create+update) -- all now use `external_field_name()`. Audited every other error-keying site in the file; no further bugs found.
  - New regression tests in `tests/fields/virtuals/{mod,ignore}.rs`. Full suite went from 529+46 to 805+46 passing.
  - Found but left alone (unrelated to aliasing): `tests/utils.rs` is also dead code, an unused duplicate of `async_test_matrix!`.
- [ ] update benchmarks, run benchmarks, compare results and improve performance
- [x] `update` didn't correctly detect "nothing to update" when the caller resubmitted a field with its *unchanged* value -- found via `examples/main_demo`'s "nothing to update" scenarios, which instead produced a "successful" update with silently mutated, unrelated fields. Root cause traced to `rs/`'s `filter_input_fields_allowed` (`rs/src/model/mod.rs:1162`), which rs-next's early nothing-to-update checkpoint never implemented: rs-next only checked "was the field provided and not ignored/readonly", never comparing the submitted value against what's already stored, so a resubmitted-but-unchanged field still counted as relevant everywhere downstream. This was a chain of gaps, not just one:
  - Checkpoint 1 (`update_field_relevant_check`): now also requires the submitted value to actually differ from `__original_output` for `#[required]`/`#[lax]` fields (virtual fields stay relevant-if-provided, matching `rs/`, since their dependent(s) haven't resolved yet). A genuinely-irrelevant field also gets `input.field` unset immediately here now (`update_relevant_flag_decls`), matching `rs/`'s immediate `input.ivo_internal_unset`, not just a boolean used for the checkpoint -- this surfaced 4 more pre-existing test-porting bugs (`tests/fields/required.rs`'s ignored/readonly on_failure tests asserted `ctx.input()` where `rs/`'s original used `ctx.raw_input()`; only visible once `input()` was actually being stripped for those fields).
  - `update_assignment_items`'s own validate gate: was `updates.field.is_some()` only; now also requires the value to differ from `__original_output`, so an unchanged field's `#[validate]` is skipped entirely (still counts as "attempted" for the final checkpoint, matching `rs/`'s `fields_provided` vs `relevant_fields_provided` distinction).
  - `re_validate` for required/lax fields (shared codegen with `create`, which needs no extra gating there): was `!errors.contains_key(name)` only, meaning it ran for *every* required/lax field on every update regardless of relevance. Refactored into `build_re_validate_items(extra_guard)`, called once for `create` (`true`) and once for `update` (the new `__update_relevant_*` flag) -- matching `rs/`'s `re_validate()`, which only iterates `relevant_fields_provided`.
  - `post_validate` groups: ran unconditionally for every configured group on every call, matching neither `create` nor `update`'s `rs/` behavior (`rs/`'s `post_validate()` only runs a group if `fields.iter().any(|f| fields_collection.is_relevant_config_name(f))`). Added a `relevance_guard_for` parameter to `build_post_validate_phase`; wired to `true` for `create` (left as a known, separate gap -- see below) and to an OR of the group's fields' relevant flags for `update`.
  - Dependent-field resolution's `parent_guard` for required/lax parents: was `updates.field.is_some() || __original_output.field != output.field`, so a parent merely *resubmitted* (even unchanged) still triggered every dependent below it -- e.g. `username_last_updated_at` getting a fresh timestamp on every update that just re-submitted the same `username`. Matches `rs/`'s `resolve_dependent_values`, which only resolves a dependent whose parent is in `relevant_fields_provided`; now reads the same `__update_relevant_*` flag directly.
  - New shared flags: `__update_relevant_{name}`, one per required/lax field, declared once (mirroring virtual fields' own `__virtual_provided_*`) so every consumer above reads the same value instead of re-deriving it (and risking drift, as `update_assignment_items`'s inline duplicate already had before this fix).
  - **Known remaining gap, deliberately not fixed here**: `create`'s `post_validate` groups still run unconditionally regardless of whether any of their fields were actually submitted (`rs/` has the same restriction on create as update -- a lax field that's only ever *defaulted*, never submitted, doesn't count as relevant there either). Left as `true` to avoid touching `create`'s well-tested behavior in this pass.
  - Verified against all 6 of `examples/main_demo`'s "nothing to update" scenarios (1/4 a/b/c/d, 3/4, 4/4) by toggling each one active and confirming "Nothing to update"; also spot-checked a mixed scenario (2 unchanged fields + 1 genuinely changed) produces the correct partial update with the unchanged fields absent from `changes`, and that a real validation failure still fails correctly (relevance gating doesn't accidentally swallow real changes).
  - Covered by `should_not_run_validate_re_validate_post_validate_or_dependent_resolution_for_unchanged_fields_even_when_the_update_is_otherwise_relevant` in `tests/extras/update_relevance.rs` (new file, wired into `tests/extras/mod.rs`) -- deliberately includes one genuinely-changed field so the test exercises each phase's *own* relevance gate, not just the early checkpoint (which alone would mask a regression in any later gate by never reaching it). Verified load-bearing by reverting the `update_assignment_items` gate specifically and confirming the test fails with the expected panic.
  - Full suite: 810 + 46 passing (up from 809 + 46), stable; `examples/main_demo` still runs end-to-end cleanly.

# added later

- [ ] limit allocations as much as possible
  - [ ] use static strs instead of `String` for schema fields
  - [ ] use arrays instead of `Vec` for schema fields where possible
- [x] Ported the virtual-field validate/re-validate/sanitize gating fix to `update` too (it had **zero** such treatment before). Tests: `should_properly_use_re_validated_values`, `should_respect_sanitizers_if_provided`.
- [x] GOAL.md §10's named-const grouped-option anchors (e.g. `const NAME_EMAIL_REQUIRED: () = ();`) now actually work -- fixed two spots in `crates/derive/src/lib.rs` that were keying off the const's name instead of whether it has a recognized option attribute. Covered by `should_allow_named_consts_as_grouped_option_anchors` in `tests/options/mod.rs`.
- [x] `#[depends_on(...)]` and `#[ivo_virtual(...)]` now require string literals (`#[depends_on("age")]`), not bare identifiers -- the only accepted form. Updated every test/example (33 files) and GOAL.md. Compile-fail coverage in `tests/field_configs/compile_fail/dependents.rs`.
