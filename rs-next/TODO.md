- [ ] [@smoke.rs (553:553)](file:///Users/mac/Documents/perso/ivo/rs-next/tests/smoke.rs#L553:553) take note of this very carefully and make sure to document this in the goal and plan, because i have corrected this before but you just keep doing your own thing. the philosophy of ivo is to move from PartialInputs to Outputs at creation and from PartialInputs to PartialOutputs during updates. the signature of the args of Model.create is `create(input: PartialInput, options: CtxOptions)` and that of Model.update is `update(existing: Output, updates: PartialInput, options: CtxOptions)`. after you document that, make sure to update all tests and examples that may be affected by the update
- [ ] default resolvers, constant resolvers, dependent resolvers, validators, re_validators, sanitizers, timetamp resolvers, on_delete handlers, on_failure handlers, on_success handlers, etc. should all expect closures that are `Fn` not `FnOnce` because they can called multiple times
