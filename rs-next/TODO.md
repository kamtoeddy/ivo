# Open

- [x] `IvoRwCtxOptions::read_only()` was fully public despite only ever being called by macro-generated code (to build the `IvoCtxOptions` handed to lifecycle hooks and returned as `ctx_options` on success/failure) -- any user validator/resolver holding an `&IvoRwCtxOptions<CtxOptions>` could call it too, even though there's no legitimate reason to. Renamed to `__read_only` + `#[doc(hidden)]`, matching the crate's existing convention for internals that must stay technically reachable from macro-expanded code living in the caller's own crate (Rust has no way to scope visibility to "generated code only" across a crate boundary) but aren't meant to be called directly. Updated the 3 derive-macro call sites (`crates/derive/src/lib.rs`). Full suite: 843 + 46, unchanged; `examples/main_demo` still runs end-to-end cleanly.
- [x] added doc comments to `IvoRwCtxOptions::{read, write, read_sync, write_sync}` and `IvoCtxOptions::{read, read_sync}` -- when to use which (async handler -> `read`/`write`; sync handler -> `read_sync`/`write_sync`), and on the two `_sync` methods specifically, a warning against calling them from inside an independently-spawned task (e.g. `tokio::spawn`) not sequenced by `ivo`'s own `emit_async_phase` ordering guarantee (see the GOAL.md note added earlier), since that's the one real way to turn the blocking calls into an executor-stall/deadlock hazard. Verified `cargo doc --no-deps -p ivo` builds clean with no broken intra-doc-link warnings. Full suite: 843 + 46, unchanged.
- [ ] manually go through all tests in `/rs` and make sure they have been ported correctly -- audited via full read-through of both `tests/` trees (see findings below); real gaps found, none critical, but should be closed:
  - [x] `tests/extras/ctx_options/virtuals.rs`: ported the 4 missing scenarios from rs's 11 (recount from the original note, which undercounted by one) -- validate-during-update, re_validate-at-creation, re_validate-during-update, sanitize-during-update -- each following the file's existing schema/assertion shape (a dependent field reading the virtual field's value, a ctx_options-writing handler, and a panic-message-as-assertion `on_failure`/`on_success` hook via `read_sync()`). Now has all 11 scenarios rs does (7 -> 11 tests, 21 -> 33 with the async-runtime matrix). Full suite: 843 + 46 (up from 831 + 46); `examples/main_demo` still runs end-to-end cleanly.
  - [x] added `tests/field_configs/compile_fail/virtuals.rs` with 8 `trybuild` cases covering both alias-collision checks in `crates/derive/src/lib.rs`'s `validate_field_names` (called before `validate_dependencies`, so these fire before the unrelated "virtual field must be referenced by a dependent" check would -- schemas below deliberately don't bother satisfying that one): duplicate alias across two virtual fields; an alias equal to the virtual field's own name (falls under the general collision check, not a bespoke message -- traced through the logic to confirm `names.contains(alias)` is trivially true for a field's own name); alias colliding with an unrelated required field; alias colliding with a dependent field that doesn't actually depend on this virtual (the `names_match` escape hatch correctly not applying); and the 4 timestamp-name variants (default/custom `created_at`, default/custom `updated_at`). rs's 8 scenarios map to just these 2 distinct rs-next error messages (rs has more granular per-scenario wording); the other scenarios in rs's file are structurally moot here per the confirmed-full-parity note above. Verified load-bearing by temporarily disabling each of the 2 checks in turn and confirming all 7 (resp. 1) affected cases fail with the "must be referenced by a dependent" error instead, causing a stderr mismatch, then restored. Full suite: 831 + 46, unchanged (all 8 cases live inside the existing single `ui` trybuild test); `examples/main_demo` still runs end-to-end cleanly.
  - [x] `field_configs/compile_fail/dependents.rs`: added the 5 missing `#[depends_on]` compile-time checks as new `trybuild` cases (empty parents array, self-dependency, unknown parent field, duplicate parent, dependency on a timestamp field -- the constant half of that last one was already covered by `lib.rs`'s own unit test `rejects_dependency_on_constant`), appended after the existing 2 so their line numbers in `dependents.stderr` stayed stable. `field_configs/dependents.rs` (non-compile_fail) needs no changes -- confirmed it's exclusively `should_allow_*`/async-resolver-behavior tests, since all 6 of these checks are compile-time-only and can't be exercised at runtime. Verified load-bearing by temporarily disabling the self-dependency check and confirming the `ui` test fails (the schema falls through into the circular-dependency detector instead, producing a different error and a stderr mismatch), then restored. Full suite: 831 + 46, unchanged (all 5 new cases live inside the existing single `ui` trybuild test, not new `#[test]` fns); `examples/main_demo` still runs end-to-end cleanly.
  - [x] `fields/virtuals/ignore.rs`: added 9 tests closing this gap. Correction to the item as originally phrased -- `#[ignore]` is never bare; it must be conditional (`#[ignore(|_, _| ...)]` or the async form). What was actually untested: (1) field-level *conditional* `#[ignore(handler)]` on a virtual field (applies to both create and update) -- added `should_respect_field_level_ignore_on_virtual_fields` + alias + alias-collides-with-dependent-name variants; (2) field-level *bare* `#[ignore_init]` (no closure allowed there -- create only, update unaffected) -- added `should_respect_field_level_ignore_init_on_virtual_fields` + the same 2 alias variants; (3) the `_with_alias_same_as_dependent` collision variant, missing for the 3 scenario types already covered (field-level `#[ignore_update]`, grouped `#[ignore]`, grouped `#[ignore_update]`) -- one test each. All 9 new tests + schemas ported from `rs/tests/fields/virtuals/ignore.rs`'s equivalent scenarios (sync only, not the sync/async matrix rs-next's existing tests in this file use -- the async path for field-level ignore/ignore_init isn't otherwise exercised by this addition, a smaller residual gap). Full suite: 827 + 46 (up from 818 + 46); `examples/main_demo` still runs end-to-end cleanly.
  - [x] ported `rs/tests/fields/lax/mod.rs::should_properly_handle_grouped_required_errors` faithfully (same schema shape, constants, and same-error-vs-different-error x create-vs-update matrix) as `should_properly_handle_grouped_required_errors` in `tests/options/required.rs`.
  - [x] fixed a real bug surfaced while porting: rs's generated `{Input}Errors::set_{field}(...)` returns `&mut Self` and chains (`.set_lax(...).set_lax_1(...)`, see `rs/crates/derive/src/ivo_struct.rs`); rs-next's `generate_errors_struct` (`crates/derive/src/lib.rs`) generated the same setter returning `()`, silently dropping the API parity every existing single-setter test call site never exercised. Fixed by returning `self` (`&mut Self`); existing call sites that ignored the return value are unaffected, and the newly-ported test now chains exactly like `rs/`'s original. Full suite: 818 + 46, unchanged; `examples/main_demo` still runs end-to-end cleanly.
  - [x] confirmed NOT a gap: `rs/tests/value_erasal.rs` (type-erasure/`erase_value` runtime layer) has no rs-next equivalent because that layer doesn't exist in rs-next's codegen-based architecture at all -- nothing to port.
  - [x] confirmed full parity: `extras/ctx_options/{constants,dependents,lax,required}.rs`, `extras/error_sanitizer.rs`, all of `options/{ignore,ignore_update,post_validate,required}.rs`'s compile-time scenarios (ported as `trybuild` cases under `options/compile_fail/*`), `fields/lax/*`/`fields/required/*` -> `fields/lax.rs`/`fields/required.rs` consolidation (bar the one gap above), `fields/virtuals/{on_failure,on_success}.rs` (rs-next is actually more thorough here), `ivo_derive.rs` -> `ivo_struct.rs`, `field_configs/{timestamps,constants,lax,required}.rs` (rs's remaining `should_reject_*` cases there are structurally moot in rs-next's single-generated-struct architecture).
  - [x] checked for internal duplication/redundancy in `rs-next/tests` (the other suspected issue) -- none found. Specifically checked: `fields/virtuals/mod.rs`'s "nothing to update" tests vs `extras/update_relevance.rs`'s relevance-gating tests (different code paths -- whole-update-is-a-no-op early exit vs. per-field gating within an otherwise-relevant update -- both legitimately needed); same-named tests reused per field-type across `fields/{lax,required}.rs`/`fields/virtuals/mod.rs` and `extras/ctx_options/*` (deliberate, mirrors rs's own one-schema-per-field-type file structure, not duplication).
- [x] `tests/utils.rs` is dead code -- not `mod`-declared anywhere in `tests/mod.rs`'s tree (confirmed via `cargo test --no-run`, which produces no separate binary for it), and is a verbatim duplicate of the `async_test_matrix!` macro that's already defined directly in `tests/mod.rs`. Delete it.
- [ ] reviewed the `llvm-cov` HTML report (93.41% fn / 92.38% line / 94.61% region overall; `crates/validators` 100%, `crates/derive/src/lib.rs` 93-96%, `src/internals/types.rs` weakest at 72-81%) to find untested code and dead-code candidates:
  - [x] `src/internals/types.rs`: `IvoContext::values()`, `full_values()`, and `previous_values()` all had the exact same body (`&self.values`) -- three names for one accessor, and `previous_values()` was actually wrong under that body (it returned the current, progressively-mutating record, not the pre-update snapshot its name promises). Fixed per explicit correction: dropped `full_values()` (its 2 call sites in `examples/{virtuals,lax}_with_ignore.rs` now use `values()`, which is what it actually meant); `previous_values()` is now `Option<&O>` (`None` at creation, `Some(&__original_output)` -- the untouched pre-update record, captured once via a new `previous_values: Option<O>` field threaded through `IvoContext::new` at all 17 call sites in `crates/derive/src/lib.rs`); `changes()` is now `Option<&O::Partial>` (`None` at creation, `Some(&__changes)` -- the actual per-field diff -- during update, instead of the full created/current record as a partial). `is_update()` is now derived from `previous_values.is_some()` instead of a separately-tracked bool, so the two can't drift. `values()` unchanged (still the full, most-up-to-date record at creation or during update). Full suite: 813 + 46, unchanged; `examples/main_demo` and all examples still build/run cleanly.
  - [ ] `PartialErrorsMethods::entries()` (and its impl on `IvoErrorPayload`) is dead code in rs-next -- confirmed via repo-wide grep it's never called anywhere (no test, no example, no derive-generated call site); it's a straight port of the trait/impl from `rs/`, but rs-next never ported the consumer (`rs/`'s own error-formatting code) that actually called `.entries()`. Either wire it into something or remove it.
  - [ ] `IvoDefaultCtx::{input,raw_input}` and `IvoConstantCtx::{input,raw_input,values}` are all `unimplemented!()` stubs -- unclear yet whether these are genuinely dead, or deliberate marker types whose methods are only used for type-level bounds during codegen and are never actually invoked at runtime (in which case `unimplemented!()` is intentional, not a gap). Needs a targeted check of where `IvoDefaultCtx`/`IvoConstantCtx` values actually get constructed and whether these methods are ever called on them.
  - [x] `IvoRwCtxOptions::write_sync()` was untested -- confirmed just a missing test, not dead code. Added `tests/extras/ctx_options/sync_write.rs`: a fully-sync validator calls `opts.write_sync().add_message(...)`, a sync `#[on_success]` hook reads it back via `opts.read_sync()` (already-tested) and panics with the message so `#[should_panic(expected = "...")]` asserts the exact value round-tripped, matching the existing panic-message-as-assertion convention used throughout `tests/extras/ctx_options/*`. Verified load-bearing by temporarily removing the `write_sync()` call and confirming the test fails (panics with "index out of bounds" instead of the expected message, since `messages` stays empty), then restored.
  - [x] neither `read_sync()`/`write_sync()` had concurrency coverage either (raised separately) -- added `tests/extras/ctx_options/sync_concurrency.rs` with 3 tests against real OS threads: multiple readers can hold `read_sync()` guards simultaneously (proven via an atomic rendezvous counter with a bounded timeout, mirroring the existing async `rendezvous()` pattern in `tests/options/required.rs` but for blocking threads instead of polled tasks); a `write_sync()` guard blocks a concurrent `read_sync()` until released, and the reader observes the writer's value afterward (proves ordering, not just eventual exclusion); a `write_sync()` guard blocks a second concurrent `write_sync()` the same way. These reach directly into `ivo::__ivo_internals::IvoRwCtxOptions` instead of going through a generated schema/model (every other test in this directory does) -- necessarily so, since `create`/`update` each own their `ctx_options` value outright per call, so two separate calls never share a lock and cross-call blocking can't be observed through the public schema API at all. Not verified via revert-and-confirm-failure like the derive-macro fixes elsewhere in this file: there's no gating logic of ours to break here, `read_blocking`/`write_blocking` are `async-lock`'s own primitives, and each test's timeout-based design is self-verifying by construction (a real regression times out and panics with a clear message rather than passing or hanging silently).
  - Full suite: 831 + 46 (up from 827 + 46); `examples/main_demo` still runs end-to-end cleanly; the 3 new concurrency tests also re-run clean across several repeats.
  - [x] confirmed low-priority/expected, not real gaps: `Debug` impls for internal wrapper types (`IvoCtxOptions`, `IvoRwCtxOptions`, `IvoTriggerFuture`, `IvoTriggerFn`, `IvoSuccessHandle`, `IvoFailureHandle`) are untested but trivial passthroughs; the `unreachable!()` arms in `IvoSuccessHandle`/`IvoFailureHandle`'s `handle_success`/`handle_failure` (mismatched Sync/Async trigger variant) are genuinely unreachable by construction (the sync/async split is enforced by the const-generic impl blocks), not missing coverage.
  - [x] `crates/derive/src/lib.rs`'s 253 uncovered lines (104 scattered ranges) are, on inspection, essentially all `syn::Error::new_spanned(...)` branches for malformed macro-attribute syntax (duplicate/unknown `#[ivo_schema(...)]` args, malformed `#[ivo_virtual(...)]` syntax, etc.) with no matching `compile_fail`/`trybuild` test -- no distinct dead code found here beyond what's already tracked above (the alias-collision and `#[depends_on]` validation gaps). Rather than cataloguing all ~90 remaining small error-branch gaps individually, recommend a systematic pass: grep every `syn::Error::new_spanned`/`syn::Error::new` message string in the file and confirm each has a `compile_fail` test asserting that exact wording.
- [ ] investigate potential performance (memory and speed) enhancements
  - [ ] limit allocations as much as possible
    - [ ] use static strs instead of `String` for schema fields
    - [ ] use arrays instead of `Vec` for schema fields where possible
- [ ] update docs (and README) of `rs-next`

# Done

- [x] `create`'s `post_validate` groups no longer run unconditionally -- added `create_group_relevance_guard`, mirroring `update_group_relevance_guard`: required/lax fields use `input.#name.is_some()` (reliable because create's defaults are only ever written to `output`, never back into `input` -- see `create_early_items`), virtual fields reuse the existing `__virtual_provided_*` flags. Wired into both `post_validate_create_pre_phase` and `post_validate_create_main_phase`. New regression tests in `tests/extras/create_relevance.rs` (negative + positive cases for both a required/lax group and a virtual-field group, the latter needing a throwaway `#[depends_on("v_trigger")]` field since every virtual field must be referenced by a dependent); verified load-bearing by neutering `create_group_relevance_guard` to always-`true` and confirming both negative tests fail as expected, then restoring it. Full suite: 817 + 46 (up from 813 + 46); `examples/main_demo` still runs end-to-end cleanly.
- [x] fixed `## 17. Execution pipeline` in GOAL.md: virtual-field validate/re-validate/sanitize gating, parallel independent phases via `emit_async_phase` (with real ordering-bug fixes: constants/dependents/timestamps), timestamp resolver called once per call, dead dependent-resolution scaffold removed for schemas with no `#[depends_on]` fields, ignore/required phase splices condensed, GOAL.md §17 rewritten to match reality. Later folded validate/re-validate/post-validate/required phases together with their fail-fast checks into single self-contained phase variables (verified via `cargo expand` that check ordering is unchanged). Full suite: 809 + 46, stable throughout.
- [x] removed bare `#[ignore]` (not a valid config); `ignore_update` now rejects empty field arrays.
- [x] fail-fast: `create`/`update` return immediately after each phase if `errors` is non-empty.
- [x] `update` now matches all 3 of `rs/`'s `handle_nothing_to_update_error` checkpoints, not just the last.
- [x] audited stale `SKIPPED:` porting notes -- both described already-supported behavior, just untested; fixed in `tests/fields/required.rs`, `tests/options/mod.rs`, GOAL.md §19.
- [x] `IvoContext::raw_input()` now genuinely distinct from `input()` (previously an alias); `input()` also strips unchanged `#[required]`/`#[lax]` fields on update, matching `rs/`. Covered by `tests/extras/raw_input.rs`.
- [x] `#[post_validate(...)]` errors on an aliased virtual field were silently dropped (keyed by internal name instead of alias) -- fixed via `external_field_name()`.
- [x] audited alias (`#[ivo_virtual("alias")]`) handling at every phase. Found `tests/fields/virtuals/{ignore,on_success,on_failure}.rs` were never `mod`-declared (~276 tests silently never ran); wired them in, which surfaced 8 pre-existing "internal name instead of alias" assertion bugs plus the same bug class at 3 more derive-macro call sites -- all fixed. Full suite: 529+46 -> 805+46.
- [x] fixed `update` not detecting "nothing to update" when a resubmitted field's value was unchanged (was only checking "provided", never comparing against stored value) -- root-caused to a missing port of `rs/`'s `filter_input_fields_allowed`. Fixed across all downstream consumers: the early relevance checkpoint, `update_assignment_items`'s validate gate, `re_validate`, `post_validate` group gating (update only, see open item above), and dependent-resolution's parent guard -- all now read one shared `__update_relevant_*` flag per field. Verified against all 6 of `examples/main_demo`'s "nothing to update" scenarios; regression tests in `tests/extras/update_relevance.rs` cover both required-field and virtual-field (unaliased / unrelated alias / alias colliding with a dependent name) trigger shapes, each verified load-bearing by reverting the relevant guard and confirming failure. Full suite: 813 + 46.
- [x] ported the virtual-field validate/re-validate/sanitize gating fix to `update` (had zero such treatment before).
- [x] GOAL.md §10's named-const grouped-option anchors now actually work (were keying off const name instead of recognized option attribute).
- [x] `#[depends_on(...)]` and `#[ivo_virtual(...)]` now require string literals, not bare identifiers -- updated across 33 files + GOAL.md, with compile-fail coverage.
