- [x] fix `## 17. Execution pipeline` in GOAL.md
  - [x] virtual fields' validate/re_validate/sanitize now correctly gated: skipped unless provided & not ignored (validate/re_validate), and sanitize only runs after `post_validate` succeeds. See `build_virtual_pipeline` in `crates/derive/src/lib.rs`; covered in `tests/fields/virtuals/mod.rs`.
  - [x] every independent-handler phase in `create`/`update` now runs in parallel via `emit_async_phase` (sequential for 0/1 async handler, `join!`-batched for 2+, matching GOAL.md §15): validate+re-validate (required/lax and virtual merged into one phase each), dependent resolution, constants, timestamps, lifecycle hooks, `post_validate` (two-phase batching matching `rs/`), ignore evaluation, required evaluation. Also fixed real pre-existing ordering bugs along the way: constants were attached before dependents resolved, and timestamps before constants (both now correctly ordered per GOAL.md §17). Each phase has a dedicated rendezvous-based concurrency-proof test in the relevant `tests/fields/*` file.
  - [x] timestamp resolver now called at most once per `create`/`update` call (previously once per field). Covered by `should_call_the_timestamp_resolver_at_most_once_per_create_call` in `tests/fields/timestamps.rs`.
  - [x] `dependent_create_block` (create's dependent-field resolution) was hand-built as an unconditional `if/else` that always emitted a `HashSet` declaration + resolution loop, even for schemas with zero `#[depends_on]` fields -- the loop degenerated to a no-op (breaks on its first iteration), but the scaffold was still generated and compiled, contradicting GOAL.md §17's "a schema with no `#[depends_on]` fields does not generate the dependent-resolution loop" guarantee. Its update-side sibling (`dependent_update_assignments`) was already correctly empty in this case (it's a plain `Vec<TokenStream>`, one entry per dependency level, naturally empty with no levels). Fixed by short-circuiting `dependent_create_block` to `quote!{}` when there are no dependent fields. Verified via `cargo expand` on a zero-dependent-field schema, before/after the fix, confirming the scaffold is actually gone from the expansion (a compile-only check wouldn't catch this, since the old code was already correct at runtime, just wasteful in codegen). Audited every other phase-producing variable against GOAL.md §17's "Dynamic pipeline generation" checklist (constants, timestamps, ignore/ignore_init/ignore_update, required, validate, re-validate, post_validate, sanitize) -- all already correctly collapse to `quote!{}` when inapplicable via `emit_async_phase`'s existing `items.is_empty()` early return; no further bugs found.
  - [x] condensed the ignore-phase splices (flag declarations + batched evaluation + `ignore_init`/bare-`ignore_update` overrides -- previously 3 separate splices each in `create`/`update`) and the required-phase splices (batched evaluation + bare-`#[required]` missing-field checks, create only) into one named phase variable each (`create_ignore_phase`/`update_ignore_phase`, `create_required_phase`), for readability -- pure codegen-structure change, no behavior difference (each constituent was already correctly empty-when-inapplicable, so the combined variable is too).
  - [x] rewrote GOAL.md §17's Create/Update phase lists to match the actual current pipeline order precisely (they'd drifted from reality across many earlier fixes this session): fail-fast noted after every phase, `post_validate` split into its real pre/main two-phase batching, and update's "evaluate update validity" corrected from appearing after *every* phase (validate, re-validate, post-validate, sanitize individually, as the stale doc claimed) to what the code and `rs/` both actually do -- once, right after post-validate. All 3 update nothing-to-update checkpoints are now called out by number matching `rs/`'s `handle_nothing_to_update_error`.
  - Full suite after all of the above: 809 + 46 passing, unchanged (pure refactor + dead-code removal, no intended behavior change); `examples/main_demo` still runs end-to-end cleanly.
  - [ ] **Deferred, larger follow-up (explicitly scheduled last by request)**: merge each of validate/re-validate/post-validate-pre/post-validate-main with their trailing fail-fast error check into one self-contained phase variable (instead of the caller splicing `#create_error_check`/`#update_error_check` separately after each), and move virtual-field "provided" determination from the current upfront, shared `setup_stmts` block (in `build_virtual_pipeline`) into each phase's own construction -- matching `rs/`'s `validate()`/`re_validate()` (`rs/src/model/mod.rs:700`, `:800`), which are each a single self-contained function computing relevance, running validators, and checking errors together. Needs its own investigation first: every downstream reader of `__virtual_provided_*` (sanitize, dependent-resolution guards, the mid-pipeline nothing-to-update check) has to be audited before moving where those flags get computed. Not started.
- [x] Bare `#[ignore]` is not a valid config; removed all associated logic.
- [x] `ignore_update` rejects empty field arrays; a closure with no fields is the entity-level handler.
- [x] fail-fast: `create`/`update` now return immediately after each phase (missing-required, validate, re-validate, post-validate pre+main) if `errors` is non-empty, instead of letting later phases run against already-invalid data. Covered in `tests/fields/required.rs`.
- [x] `update` now matches all 3 of `rs/`'s `handle_nothing_to_update_error` checkpoints (right after ignore/readonly filtering; after post_validate strips unchanged fields; after dependent resolution), not just the last one. Covered in `tests/fields/required.rs`.
- [x] audited all `SKIPPED:` ports; both were stale notes, not real design gaps -- the described behavior was already supported, just never ported/tested. Fixed in `tests/fields/required.rs` and `tests/options/mod.rs`/GOAL.md §19 (`on_success`'s empty-array form is deliberately rejected; "always fire" is the bare arrayless form only).
- [x] `IvoContext::raw_input()` now tracks the schema's original partial input, genuinely distinct from `input()` (previously just an alias). `src/internals/types.rs` gained a real `raw_input` field; `crates/derive/src/lib.rs` threads the correct original-input snapshot through all `IvoContext::new(...)` call sites (create/update phases and lifecycle-hook triggers), plus strips unchanged `#[required]`/`#[lax]` fields from `input()` on update (matching `rs/`'s `evaluate_update_validity`) while `raw_input()` keeps showing what was submitted. Covered by `tests/extras/raw_input.rs`; corrected two pre-existing tests in `tests/fields/{lax,required}.rs` that had been written against the old, wrong behavior.
- [x] `#[post_validate(...)]` errors on an _aliased_ virtual field were silently dropped (allow-list filtered by internal field name, but the error payload is keyed by the alias) -- found via a real panic in `examples/main_demo`. Fixed in `crates/derive/src/lib.rs` via a new `external_field_name()` helper. Covered by `should_surface_post_validate_errors_on_an_aliased_virtual_field` in `tests/fields/virtuals/mod.rs`.
- [ ] manually go through all tests in "/rs" and make sure they have been ported correctly
- [x] audited alias (`#[ivo_virtual("alias")]`) handling at every phase per `rs/tests/fields/virtuals`. Found and fixed:
  - **Three whole test files were silently dead code**: `tests/fields/virtuals/{ignore,on_success,on_failure}.rs` were never `mod`-declared anywhere, so ~276 tests (including all existing alias coverage for those phases) never ran. Fixed by wiring them into `tests/fields/virtuals/mod.rs`.
  - Wiring them in surfaced 8 real pre-existing porting bugs in `on_failure.rs`'s alias tests (asserted the wrong error key -- internal name instead of alias). Fixed.
  - Found the same "internal name instead of alias" bug class in three more derive-macro call sites (virtual field's own validate/re_validate errors, field-level `#[required(handler)]` on virtual fields, create+update) -- all now use `external_field_name()`. Audited every other error-keying site in the file; no further bugs found.
  - New regression tests in `tests/fields/virtuals/{mod,ignore}.rs`. Full suite went from 529+46 to 805+46 passing.
  - Found but left alone (unrelated to aliasing): `tests/utils.rs` is also dead code, an unused duplicate of `async_test_matrix!`.
- [ ] update benchmarks, run benchmarks, compare results and improve performance

# added later

- [ ] limit allocations as much as possible
  - [ ] use static strs instead of `String` for schema fields
  - [ ] use arrays instead of `Vec` for schema fields where possible
- [x] Ported the virtual-field validate/re-validate/sanitize gating fix to `update` too (it had **zero** such treatment before). Tests: `should_properly_use_re_validated_values`, `should_respect_sanitizers_if_provided`.
- [x] GOAL.md §10's named-const grouped-option anchors (e.g. `const NAME_EMAIL_REQUIRED: () = ();`) now actually work -- fixed two spots in `crates/derive/src/lib.rs` that were keying off the const's name instead of whether it has a recognized option attribute. Covered by `should_allow_named_consts_as_grouped_option_anchors` in `tests/options/mod.rs`.
- [x] `#[depends_on(...)]` and `#[ivo_virtual(...)]` now require string literals (`#[depends_on("age")]`), not bare identifiers -- the only accepted form. Updated every test/example (33 files) and GOAL.md. Compile-fail coverage in `tests/field_configs/compile_fail/dependents.rs`.
